# Лендинг-приглашение на день рождения

React + Supabase лендинг с подтверждением участия.

## Что делает

- Открывает приглашение по ссылке вида `?invite=<token>`.
- Если `invite` в URL отсутствует, создает его автоматически.
- При первом открытии активирует таймер и ставит срок действия +15 часов.
- Сохраняет ответ гостя (`Да, я приду` / `Не смогу`) и имя.
- Telegram-ссылка показывается только если гость нажал `Да, я приду`.
- Если за 15 часов нет ответа, фиксируется автоматический отказ.

## Быстрый старт

1. Установить зависимости:

```bash
npm install
```

2. Создать `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

3. В Supabase SQL Editor выполнить `supabase/schema.sql`.

4. Запустить локально:

```bash
npm run dev
```

## Как отправлять ссылку

```text
https://ваш-домен/
```

При первом открытии ссылка автоматически получит `?invite=...`, и с этого момента начнется отсчет 15 часов.
